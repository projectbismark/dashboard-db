create table fordave_pings as select * from measurements where timestamp between X and Y and param = 'ping';

sele 

insert into table pings select param as 'ping', * from oldbigtable where parm = "ping";
