select deviceid, sum(avg) as avg, sum(bla) as bla from measurements where deviceid = 'bla'  and timestamp between X and Y and param = 'bla';

select 

insert into table pings select param as 'ping', * from oldbigtable where parm = "ping";
